# v0.52.0 evidence

This directory contains the versioned result schema and certified raw LocalFS
and MinIO reports for the v0.52.0 scale matrix. Generated JSON and JSONL files
belong here and must be produced by the exact tagged source commit.

The runner does not claim a scale envelope until the result digest, recovery
checks, and bounded-memory budgets have been reviewed for every matrix entry.

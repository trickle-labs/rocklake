# v0.63.7 — Local release certification

## Delivered

- Extended `rocklake-evidence` with package-derived release identity, repeated
  raw samples, digest mismatch failures, exact ranged-read accounting, and a
  streaming export counter.
- Added the required 10,000/100,000 LocalFS candidate gate and report validator.
- Made the populated quickstart artifact-only when `ROCKLAKE_BINARY` is set.
- Advanced current release, binding, compatibility, and operator metadata to
  v0.63.7 without changing persisted catalog formats or the stable surface.

## Verification

```bash
cargo fmt --all -- --check
cargo test -p rocklake-evidence
cargo test --workspace
python3 scripts/validate_compatibility_manifest.py
mkdocs build --strict
ROCKLAKE_BINARY=target/release/rocklake bash scripts/quickstart.sh
```

The optional million-file, MinIO, emulator, and long mixed-workload profiles
remain machine-dependent; omitted raw reports do not expand the supported
envelope.

# v0.63.2 plan: honest baseline and evidence gate

## Delivered

- Reconciled support, compatibility, capacity, and performance claims with the
  evidence present in the checkout.
- Marked v1.0 release candidates and remote-cloud scale work unscheduled.
- Added a candidate-specific Criterion smoke report and a reviewed baseline to
  the release gates.
- Made missing, empty, nonfinite, incompatible, and regressed measurements fail
  the benchmark check.

This release changes no persisted catalog format or stable runtime surface.

## Verification

```bash
python3 tests/test_benchmark_regression.py
python3 scripts/run_benchmark_candidate.py \
  --output target/benchmarks/v0.63.2-candidate.json
python3 scripts/check_benchmark_regression.py \
  --candidate target/benchmarks/v0.63.2-candidate.json \
  --baseline benchmarks/v0.63.2-baseline.json
python3 scripts/validate_compatibility_manifest.py
mkdocs build --strict
```

# v0.63.5 — Remove measured local scaling obstacles

Implemented the scoped T13–T15 work:

- Cleanup collects data/delete-file retirement evidence once per operation.
- Versioned-name overlap verification sorts intervals before checking them.
- PG-wire separates cumulative response size from in-flight bytes and enforces
  the latter with cancellation-aware permits held through delivery.
- Legacy `stream_queue_depth` and `max_buffered_rows` remain accepted without
  pretending to provide runtime backpressure.
- Maintenance `run` and CLI job `resume` report unsupported worker contracts;
  ledger resume requires a safe checkpoint and foreground failures classify
  permanent errors as non-retryable.

Verification:

- `cargo fmt --all -- --check`
- `cargo clippy --workspace --all-targets --all-features -- -D warnings`
- `cargo check -p rocklake-pgwire -p rocklake-catalog`
- `cargo test -p rocklake-catalog verify::tests::sorted_name_intervals_find_overlaps_beyond_adjacent_rows`
- `cargo test -p rocklake-catalog jobs::tests::job_lifecycle_persists_checkpoint_and_resume`
- `cargo test -p rocklake-pgwire --test v051_resource_observability_tests`
- `cargo test --workspace`

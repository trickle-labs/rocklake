# v0.62.0 plan: public surface, distribution, and documentation freeze

## Delivered

- Added `rocklake support bundle --output <directory>` with non-overwriting
  output, redacted configuration, status, verification, and optional metrics.
- Expanded the machine-readable public-surface manifest with support levels,
  deprecation rules, distribution artifacts, provenance, and bundle schema.
- Updated release and operator documentation to v0.62.0 and checked in shell
  completions for the frozen CLI.

## Verification

```bash
cargo fmt --all -- --check
cargo test -p rocklake-pgwire --test v046_cli_tests --test v0478_surface_manifest_tests --lib
cargo test --workspace
mkdocs build --strict
```

OCI publication and detached signatures remain deferred because their trust
and rotation contracts are not yet certified.

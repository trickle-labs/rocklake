# v0.63.0 plan: feature-complete production beta

## Delivered at beta entry

- Bumped the workspace and binding metadata to v0.63.0 without changing a
  persisted catalog format or the frozen public surface.
- Published the production-beta support boundary, escalation route, severity
  policy, and evidence ledger.
- Added release certification checks for the beta policy and evidence ledger.

## Observation work

The v0.63.0 tag starts the 30-day design-partner observation window. The ledger
records single-catalog and multi-catalog workloads, recovery exercises,
monitoring, incidents, operator surprises, and unresolved limits. It must not
contain customer data.

## Verification

```bash
cargo fmt --all -- --check
cargo test --workspace
mkdocs build --strict
python scripts/validate_compatibility_manifest.py
bash scripts/quickstart.sh
```

AWS S3, GCS, and Azure scale certification remains deferred to v0.64.0–v0.64.2.
The v0.63.1 readiness audit owns beta findings and the final RC entry decision.

# Kubernetes

Kubernetes deployment is not a supported v0.51.2 release integration. No
RockLake container image or Helm chart is published. Use the tested binary
path, or add and maintain the required image and smoke-test workflow before
claiming Kubernetes support.

# Multi-region deployment

Multi-region deployment is not a supported v0.51.3 release integration. The
supported model is one writer and optional read-only readers sharing a catalog
through the configured object store. Validate any provider replication design
outside the RockLake release support claim.
